import { Restaurant as RestaurantContainer } from '@containers';
/**
 * Renders the Restaurant page.
 * @returns JSX.Element - The rendered Restaurant page.
 */
export const Restaurant = () => <RestaurantContainer />;
