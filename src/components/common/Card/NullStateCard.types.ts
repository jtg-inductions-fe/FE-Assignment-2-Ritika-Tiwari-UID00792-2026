/**
 * Interface defining the properties passed in the Null State Card.
 */
export interface NullStateCardProps {
    // Title displayed on the null state card.
    title: string;
    // Description of the null state card.
    description: string;
}
